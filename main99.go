package main

import (
	"fmt"
)

func main() {
	var x int
	var y int = 0
	fmt.Print()
	fmt.Scanln(&x)
	if x < 0 {
		fmt.Println("Некорректный ввод")
		return
	}or i := 1; i <= x; i++
	f {
		if i%3 == 0 {
			continue
		}
		y = y + i
	}
fmt.Println(y)
}